# ✅ Reddit RSS Migration Complete!

## Что изменилось

Теперь мониторинг Reddit работает через **RSS ленты** вместо API:

✅ **Не нужны credentials** (client_id, client_secret)
✅ **Нет ограничений API**
✅ **Работает сразу без регистрации**
⚠️ **RSS не показывает комментарии** (только посты)
⚠️ **Нет информации о score/upvotes**

---

## Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

Теперь используется:
- `feedparser` вместо `praw`
- `requests` для HTTP запросов

### 2. Конфигурация

В `config.yaml` секция Reddit теперь выглядит так:

```yaml
reddit:
  # Нет API credentials - используем RSS!

  subreddits:
    - name: "immigration"
    - name: "USCIS"
    - name: "askimmigration"
    # ... добавьте нужные

  posts_limit: 50
  check_interval_minutes: 15
```

**Просто добавьте нужные subreddit в список `subreddits`!**

### 3. Запуск

```bash
# Проверить один раз
python main.py --once

# Запустить постоянно
python main.py

# Посмотреть статистику
python main.py --stats

# Тест уведомления
python main.py --test-notify
```

---

## RSS ссылки для ручной проверки

Вы можете открыть эти ссылки в браузере для проверки:

- https://www.reddit.com/r/immigration/new/.rss
- https://www.reddit.com/r/USCIS/new/.rss
- https://www.reddit.com/r/askimmigration/new/.rss
- https://www.reddit.com/r/h1b/new/.rss

---

## Как работает RSS

- Скрипт запрашивает RSS ленту: `https://www.reddit.com/r/{subreddit}/new/.rss`
- Парсит XML с постами
- Фильтрует по времени (lookback_hours)
- Классифицирует и отправляет уведомления

**Лимиты:** Reddit не ограничивает RSS запросы так строго, как API.

---

## Что делать, если нужны комментарии?

RSS не показывает комментарии. Варианты:

1. **Мониторить только посты** (уже работает)
2. **Подать заявку на Reddit API** и дождаться одобрения (~7 дней)
3. **Использовать сторонние сервисы** типа Pushshift (если доступны)

---

## Устранение проблем

### Ошибка "HTTP 403"
Reddit может блокировать запросы без User-Agent. Скрипт уже настроен с правильным User-Agent.

### Пустые результаты
- Проверьте, что subreddit существует
- Проверьте `posts_limit` и `check_interval_minutes` в config.yaml

### Нет уведомлений
- Убедитесь, что Telegram bot настроен (секция `telegram` в config.yaml)
- Проверьте classifier (секция `classification`)
- Запустите `python main.py --test-notify` для теста

---

## Контакты и поддержка

Если что-то не работает:
1. Проверьте логи в `logs/monitor.log`
2. Запустите с `--once` для отладки
3. Убедитесь, что все библиотеки установлены: `pip install -r requirements.txt`

**Готово! Теперь можно запускать без Reddit API! 🚀**
